{
  "scripts": {
    "cy:open": "cypress open",
    "cy:runMainMode": "cypress run --e2e --browser chrome --reporter mochawesome",
    "cy:runTestMode": "cypress run --e2e --no-runner-ui --browser chrome --config-file cypress-test.config.js --reporter mochawesome",
    "cy:runApiTests": "cypress run --spec 'cypress/e2e/qauto/homeWork-21.1' --no-runner-ui --browser chrome"
  },
  "devDependencies": {
    "cypress": "^13.17.0"
  },
  "dependencies": {
    "cypress-mochawesome-reporter": "^3.8.2",
    "cypress-plugin-api": "^2.11.2",
    "cypress-plugin-tab": "^1.0.5"
  }
}
